{
  "name": "m-wallet",
  "version": "0.9.0-beta.2",
  "private": true,
  "description": "M-Wallet — a local-first personal budgeting PWA (Zevaryn Systems).",
  "scripts": {
    "test": "node --test tests/*.test.js tests/**/*.test.js"
  }
}
